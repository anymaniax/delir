import type { NextPage } from 'next';
import styles from '../styles/Home.module.css';

const Detail: NextPage = () => {
  return (
    <h1 className={styles.title}>Welcome to detail page</h1>
  )
};

export default Detail;
